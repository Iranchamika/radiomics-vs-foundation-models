# Python dependencies for the reproducibility pipeline.
# Tested with Python 3.10 and 3.11 on Windows 10/11 and Ubuntu 22.04.

# Core
numpy>=1.24
pandas>=2.0

# Plotting / figures
matplotlib>=3.7

# PDF generation (supplementary files, source-code PDF)
reportlab>=4.0

# Word-document export (cover letter, if rebuilding)
python-docx>=1.0

# HTTP — for 30_fetch_pubmed_authors.py
requests>=2.31

# R-side requirements (install separately in R):
#   install.packages(c("meta", "metafor", "dplyr", "ggplot2", "readr"))
